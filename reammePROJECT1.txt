
to do with the getr random quote project in treethosue









1. make scale able number work with a variable withing printQuote that wills ave the number and check WITHIN the scaleAbleNumber function 
   if the function called by  the formula is the same as laswt time



2  make  a function to take two arguments ande return a random number between them.
		possible solution is to take the same formula and modify it


3 standatise the code g


4 make interva l reset with every call of printQuote 

:
